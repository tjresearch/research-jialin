git add Graph.png
git commit -m "some message"
git push

rm -rf .git
git init
git remote origin add (link to repo)
git add file
git commit -m "some message"
git push --force --set-upstream-to origin master

add entire folder
git add .
git add foldername
